spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bubli}
    driver-class-name: org.postgresql.Driver
    username: ${SPRING_DATASOURCE_USERNAME:${DATASOURCE_USERNAME:bubli}}
    password: ${SPRING_DATASOURCE_PASSWORD:${DATASOURCE_PASSWORD:bubli1234}}

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}

  ai:
    model:
      chat: ${AI_CHAT_PROVIDER:bedrock-converse}
      embedding: ${AI_EMBEDDING_PROVIDER:bedrock-titan}
    vectorstore:
      type: ${AI_VECTORSTORE_TYPE:pgvector}

jwt:
  secret: ${JWT_SECRET:CHANGE_ME_AT_LEAST_32_BYTES_RANDOM_STRING}
  access-token-expire-ms: ${JWT_ACCESS_TOKEN_EXPIRE_MS:3600000}
  refresh-token-expire-ms: ${JWT_REFRESH_TOKEN_EXPIRE_MS:1209600000}

aws:
  region: ${AWS_REGION:ap-northeast-2}
  access-key: ${AWS_ACCESS_KEY_ID:}
  secret-key: ${AWS_SECRET_ACCESS_KEY:}
  s3:
    bucket: ${AWS_S3_BUCKET:${S3_BUCKET_NAME:}}
  bedrock:
    chat-model: ${BEDROCK_CHAT_MODEL_ID:anthropic.claude-haiku-4-5}
    embedding-model: ${BEDROCK_EMBEDDING_MODEL_ID:amazon.titan-embed-text-v2:0}

storage:
  type: ${STORAGE_TYPE:s3}
  local:
    base-path: ${STORAGE_LOCAL_BASE_PATH:./local-storage}

livekit:
  api-key: ${LIVEKIT_API_KEY:}
  api-secret: ${LIVEKIT_API_SECRET:}
  server-url: ${LIVEKIT_SERVER_URL:}

google:
  oauth:
    client-id: ${GOOGLE_OAUTH_CLIENT_ID:}
    client-secret: ${GOOGLE_OAUTH_CLIENT_SECRET:}
    redirect-uri: ${GOOGLE_OAUTH_REDIRECT_URI:}
  calendar:
    client-id: ${GOOGLE_CALENDAR_CLIENT_ID:}
    client-secret: ${GOOGLE_CALENDAR_CLIENT_SECRET:}
    redirect-uri: ${GOOGLE_CALENDAR_REDIRECT_URI:}

cors:
  allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:3000,http://localhost:5173,http://localhost:1420}
